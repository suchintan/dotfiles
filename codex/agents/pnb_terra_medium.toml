name = "pnb_terra_medium"
description = "Pinned Terra medium plan-and-build leaf."
model = "gpt-5.6-terra"
model_reasoning_effort = "medium"
sandbox_mode = "danger-full-access"
developer_instructions = """
Execute exactly the one scoped plan-and-build leaf task supplied by the parent. Do not spawn or delegate descendants. Treat the worktree and supplied artifact paths as authoritative. Return only the requested structured result.
"""
